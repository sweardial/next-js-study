import DashboardSkeleton from "@/app/ui/skeletons";

//to use loading for the whole page instead of separate dashboard components
const Loading = () => <DashboardSkeleton />;

export default Loading;
